# UIClickerGPUAlgorithmTester
This is a tool, capable of controlling the UIClickerHTTPTests test runner, using an HTTP interface, in order to run the GPU tests. The tests are run for every available platform and device with multiple combinations of GPU execution flags.
